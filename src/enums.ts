export enum Collection {
  USER = 'User',
  PRODUCT = 'Product',
  CATEGORY = 'Category',
  ORDER = 'Order',
  ORDER_PRODUCT = 'OrderProduct',
}

export enum Field {
  PRODUCT = 'product',
  ORDER = 'order',
  USER = 'user',
}
