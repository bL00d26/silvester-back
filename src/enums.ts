export enum Collection {
  USER = 'User',
  PRODUCT = 'Product',
  CATEGORY = 'Category',
  ORDER = 'Order',
  ORDER_PRODUCT = 'OrderProduct',
  STORE = 'Store',
}

export enum Field {
  PRODUCT = 'product',
  ORDER = 'order',
  USER = 'user',
}
export const passwordSalts = 10;
